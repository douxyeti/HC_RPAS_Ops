# Module de test — invocation/routage (local)
Ce module sert à valider l’invocation et le routage sans dépendre de Firebase.

## Lancer en local (sans Firebase)
```bash
python modules/test_module/main.py
```
